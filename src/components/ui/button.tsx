/** Compatibility re-export — canonical button lives in `src/shared/ui`. */
export {
  Button,
  buttonVariants,
  type ButtonProps,
} from "@/shared/ui/button";

export const COMPAT_BUTTON_MODULE = "components-ui-button";
