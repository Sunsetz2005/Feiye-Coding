# Sunsetz 全工作台长期重构执行状态

本文是长期重构的执行入口。产品行为仍以 `docs/llm-wiki/` 下的权威说明和代码为准；参考截图只提供可见交互事实，不覆盖 Sunsetz 的 Host、Runtime、品牌和信息架构。

## 阶段门禁

每一阶段必须依次完成：

1. 协议和持久化兼容检查。
2. TypeScript 类型检查与前端测试。
3. UI 生产构建与 Rust 测试。
4. 目标尺寸截图回归。
5. 键盘、焦点、缩放和系统辅助偏好检查。
6. 相关平台手动验收。
7. 权威文档同步。

未满足全部门禁时，阶段状态只能是“进行中”或“受限”，不得标记为完成。

## 当前状态

产品内核已改为 Sunsetz 内建 agent loop（`src-tauri/src/agent_loop.rs`）。默认不再 spawn `grok agent stdio`；Grok ACP 适配器保留在 `runtimeBackend=grok_acp` 显式开关后。Host 工具现为可信项目根内的 `read_file`、`list_directory`、`grep`、`write_file`、`search_replace`、`run_command`，以及父会话 `spawn_agent` / `agent_output` / `kill_agent` 和 `command_output` / `wait_commands` / `kill_command`。子代理是进程内 `run_turn`，深度 1。写入、替换和命令走现有权限条，`AcceptEdits` 不自动放行命令，相同工具加相同参数连续 3 次会被打断，逃出根目录/拒绝/Stop 无副作用。父会话 `run_command` 可后台托管；Stop 不杀掉已批准的后台命令。父回合只 join 本轮后台 child；Steer/Stop 只取消本轮 child；后台子代理或后台命令结束后 Host 可开一轮无用户气泡的 wake turn。前台命令墙钟 15 分钟；模型 SSE 按 `streamStallSeconds` 空闲失败，没有 2 分钟总超时。工具轮次用尽后成功结束，可再发送。显式 Skill 从库存已信任目录装载有界 `SKILL.md` 到该轮提示，journal 只留 token；不依赖 grok 二进制，GROK_HOME 不是产品内核。已审阅 Memory pack 可显示在输入器并显式注入该轮提示。信任项目会有界读取 `AGENTS.md` / `Sunsetz.md` / `.sunsetz/instructions.md` / `CLAUDE.md`。单轮最多 16 次工具调用。默认内核在 `/compact` 或占用/硬顶阈值触发时摘要模型上下文，可见 journal 不改写。`run_command` 可按 `sandboxProfile` 使用 Linux bubblewrap、macOS sandbox-exec 或 Windows AppContainer。内核可 `list_skills` / `view_skill` / `skill_save`，并有独立的自动记忆契约。`schedule_task` 写入现有已安排账本；默认内核到期由 Host 点火。设置向导是登录 Sunsetz 或跳过进入工作台，不再把 grok CLI、xAI 密钥或本机 Open Connector 当作首屏门槛。这不是整阶段完成。

## 阶段状态

| 阶段 | 状态 | 当前证据 | 仍缺少 |
|------|------|----------|--------|
| 0 基线与防回退 | 自动门禁通过 | ACP golden、会话/附件/权限测试、152 个 Tauri command 与 25 个 event 契约扫描、519 个前端测试、319 个 Rust 测试通过（另 1 个夹具生成测试按设计忽略），三主题三尺寸截图基线 | 后续涉及原生能力时补 macOS/Windows 手测；阶段零本身无新增平台行为 |
| 1 工作台骨架 | 进行中（自动门禁与 macOS 原生证据已建立） | `SidebarNavigator` 已从 `App.tsx` 抽离；项目/任务披露与当前项语义已有组件测试，统一虚拟行高和可见焦点已落地；`WorkbenchShell` 在面板关闭后恢复触发器焦点并保持中栏挂载与滚动；`HostCapabilities v2` 对未知或不可用能力保持隐藏；前端与 Rust 覆盖率基线、资源面板三主题三尺寸截图和 200% 基本几何检查已建立；当前提交重新构建的 macOS 调试 `.app` 已在 `backingScaleFactor=2.0` 的 3024×1964 Retina 屏幕确认侧栏/资源面板关闭后的焦点恢复、资源内容卸载、侧栏 Space 重开和无面板重叠；Windows CI 原生 WebView 诊断已确认工作台位于系统工作区内、面板卸载、触发器焦点恢复和侧栏 Space 重开；另有完整键盘路径与中性项目选中态证据 | 2026-08-31 物理 Windows 11 第一次实机：`3f96d3a` 1 PASS / 6 FAIL。2026-09-01 针对 `3703486` 的第二次实机仍未通过：指定提交原版 Windows 编不过；演练 exe 含未提交补丁；keyboard 在建议卡之后焦点环消失；display 截图含账户信息；responsive / themes 未覆盖完。已跟进 `8dc7a73`（Windows 0.61 编译）和随后的内侧焦点环修复。阶段一不得标记完成；须用包含这两项的干净 main 构建重跑七项 |
| 2 输入器与浮层 | 进行中 | 三层输入器、单一浮层、模型能力门控、精确上下文、附件和队列；上下文环具有悬停/聚焦摘要和点击详情；计划模式位于访问权限右侧并可原位关闭，保存失败回滚；新任务项目条只有内部文字按钮响应；900×600 不再被侧栏遮挡；`ComposerDock` 已从 `App.tsx` 抽离，拥有底部浮层、三层输入器和运行中的 `TaskProgressRail`，组件测试覆盖发送/停止、队列、进度轨、权限槽和 AskUser 接管；已审阅 Memory 徽章、项目说明芯片、`skill-v1` 草稿绑定和版本化草稿/队列恢复已落地；离线模型目录改用 Runtime 稳定 alias。数据、发送、偏好和 Host 协调仍由 `App.tsx` 提供 | 补齐有精确 Runtime 遥测时的原生窗口视觉证据 |
| 3 消息与提问 | 进行中 | `InteractionSnapshotV1` 已统一前后台 permission/ask-user/plan 生命周期；write-first、失败重试、陈旧 RPC、WebView reload 和 interrupted 审计已落地；`ConversationThread`、`ActivityTimeline`、`AskUserDock`、phase golden 保持 | 进程退出后的 RPC 按设计不可回复；仍需 Windows 实机验证切换、重载与原生通知路径 |
| 4 计划与步骤 | 进行中 | Plan 已进入统一 interaction reducer；底部 `AskUserDock` 是批准、请求修改和放弃的唯一入口；`PlanArtifactV1` 已把 approved / executing / done 做成与 Permission 分离的可恢复产物 schema，serde 终态仍是 `completed`；批准后保留正文且不自动打开资源面板；`TaskProgressRail` 只展示真实步骤 | Windows 实机仍不是本切片；产物恢复与 permission 分离已落地，阶段本身未完成 |
| 5 侧栏预览与菜单 | 进行中 | 任务菜单按按钮或指针锚定，并将现有重命名、移动到项目、导出、分叉/回退/复制和归档/删除分组；移动到项目级联列出已有项目和「不归属项目」，走 `session_set_project`；账户菜单只展示真实账户、额度、主题、设置和登录状态；任务悬停 450ms 后通过有界 `session_preview` 读取最近用户/assistant 可见摘要，键盘聚焦立即提供同等预览；项目预览显示真实任务数、活动数、路径、信任、固定、更新时间和惰性 Git 摘要；滚动、折叠、菜单打开、行卸载与过期响应均关闭；三尺寸截图已建立 | macOS/Windows 原生窗口验收 |
| 6 资源与审阅 | 进行中 | Files、Changes、Plan、diff；静态 HTML sandbox+CSP；全局 asset protocol 已禁用；ResourceHandle 与受控 media provenance 已落地 | 交互式 HTML 容器、大文件虚拟化、全部 legacy media 调用点迁移和 Windows WebView 实机证据 |
| 7 设置中心 | 进行中 | 常规、外观、账户、扩展、Runtime、归档、关于已进入统一 `SETTINGS_REGISTRY`；搜索可以通过具体配置项定位栏目，空结果本地化且不保留空分组；配置更新已迁移到白名单 `settings_patch_v1`，前端按字段 sequence 处理权威响应和失败回滚；Keychain 偏好由共同事务锁串行化；三尺寸视觉基线已建立 | 将账户与使用情况、扩展与连接、只读快捷键按真实后端能力拆分；legacy `settings_set` 仅保留受限兼容周期，后续按版本移除 |
| 8–14 平台与扩展能力 | 增量迁移进行中 | RuntimeCapabilities/EventEnvelope、Linux/macOS/Windows `run_command` 沙箱、只读插件目录/hooks、FTS5 检索、有限 Memory 候选及显式可见注入、`agent-memory.v1`、Skill candidate 治理 v2 与 `list_skills`/`view_skill`/`skill_save`、带 missed-run policy、heartbeat、Host 点火、`schedule_task` 与进程终止证明 replacement CAS 的 Rust 自动化账本、可选 OS 常驻注册、clean-room manifest 已实现；父会话后台 `run_command` 与完成后 wake 已落地；无法验证目标的应用内插件卸载已 fail-closed；2026-09-04 评估结论为保留 Grok ACP 显式开关，本周期不删除适配器，Runtime 页标明 mock/环境变量覆盖；`session_manager/mod.rs`（9567 行）已按真实职责拆成 `types` / `connect` / `acp_event` / `live` / `turn` / `interactions` 六个子模块加 `test_support`，纯代码搬迁、无行为变化，`cargo test --offline --lib -- session_manager` 39/39、全仓 `cargo test --offline --lib` 582/1 与拆分前逐字节一致，全部 ~98 处 `self.inner.lock()` 调用点已过一遍锁审计未发现嵌套死锁路径；命令 job 事件面已落地——`command_jobs.rs` 在 job 注册和终态转换（完成/失败/kill 触发的取消）时通过新增 `CommandJobEventFn` 回调发射 `session://command_job_v1`（sessionId/jobId/status/command/summary，不带完整 stdout），只读命令 `session_command_jobs_list_v1` 返回同口径的会话级 job 摘要；`cargo test --offline --lib -- command_jobs` 覆盖成功/失败/取消三条事件路径与 `list_for_session` 的会话隔离，全仓 `cargo test --offline --lib` 586/1；前端尚未订阅，composer pill 与后台待批准横幅仍是后续切片；`monitor` 行唤醒工具已落地——`execute_run_command_timed` 从进程退出后一次性 `read_to_end` 改为逐行流式读取 stdout/stderr（与等待进程退出并发，不再是退出后才读），hosted job 的 `command_output` 中途查询现在也能看到部分输出；新增父会话专属 `monitor(id, pattern?)` 工具订阅一个仍在运行的 job，匹配（或任意，未给 `pattern` 时）新行经与 job 完成 wake 相同的"跳过 `prepare_user_send`"路径唤醒父回合，内容通过新增 `command_jobs::monitor_wake_prompt` 注入 wake_context；单订阅最多 20 次唤醒后自动 detach，不是 OS 常驻；`cargo test --offline --lib -- agent_loop command_jobs` 新增流式读取、pattern 匹配/无 pattern 匹配、未知/已终态 job 拒绝、唤醒预算耗尽自动 detach 等用例，全仓 `cargo test --offline --lib` 593/1（较前一次净增 7），`pnpm typecheck`/`pnpm test`（782 通过）/`cargo build`/契约（179/25，未新增 Tauri 命令或事件，数字不变符合预期）均通过 | 同会话循环、输入器托管计数 pill 与后台待批准横幅（前端接线）、机器可读插件安装/卸载、legacy event 移除、ACP 删除立项与 Windows 物理机验收 |

## 自动化入口

- `pnpm verify:contracts`：检查前端 invoke 与 Tauri 注册命令、前端监听事件与 Host 发射事件是否匹配。
- `pnpm test:coverage && pnpm coverage:audit`：生成前端覆盖率并阻止相对阶段一基线超过 0.25 个百分点的回退。
- `pnpm coverage:changed`：按 80% 行、70% 分支强制检查改动代码覆盖率；本轮当前实测为 90.41% 行、86.05% 分支。
- `pnpm coverage:rust && pnpm coverage:rust:audit`：Rust 行覆盖率当前实测 44.39%，通过 37.62% baseline gate；最终覆盖率目标仍单独追踪。
- `pnpm test:visual`：运行三种尺寸、三种主题的空工作台与资源面板截图回归，覆盖上下文摘要、计划模式、唯一计划决策入口、侧栏预览和菜单，并执行 200% 基本可操作性检查。
- `pnpm test:visual:ci`：在 Linux 执行同一行为和几何矩阵，但忽略仅适用于 Darwin 的像素基线。
- `pnpm test:visual:update`：仅在人工确认视觉变化符合参考事实后更新基线。
- `scripts/windows-native-smoke.ps1`：CI 中启动真实 Windows Tauri 窗口，并通过 WebView2 CDP 验证窗口工作区边界、面板生命周期、焦点恢复和 Space 键路径；产物包含原生窗口与资源面板截图。
- `scripts/windows-stage1-manual.ps1`：物理 Windows 上逐项记录 200% 缩放、完整键盘与焦点、面板生命周期、窄窗口和三主题证据；任何 FAIL 返回非零状态。
- `pnpm verify:stage0`：阶段零前端与视觉门禁；Rust 测试仍单独在 `src-tauri` 运行。

截图和通用 200% 检查使用浏览器降级路径。macOS 调试 `.app` 已补充 Retina 2× 原生窗口证据，Windows CI 已补充云端原生窗口自动证据；两者都不替代 Finder、系统权限或 Windows 实机手动验收。`coverage-policy.json` 中的全仓 80% 行 / 70% 分支是最终目标，不是当前全仓覆盖率结论。

本轮成熟能力迁移的安全边界、技术债和平台限制见 [`docs/llm-wiki/runtime-migration-v1.md`](llm-wiki/runtime-migration-v1.md)。在 Windows 物理机验收前，本轮只能标记为“自动门禁通过、发布受限”。
