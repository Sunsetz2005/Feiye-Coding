# Changelog

## [UNRELEASED] — 2026-09-08 19:35

- 模型/推理强度/模式/权限策略状态从 `App.tsx` 抽到独立的 `useComposerCatalog` hook，行为不变。
- 应用内确认/输入/编辑项目弹窗从 `App.tsx` 抽到独立的 `useAppDialog` hook 与 `AppDialogHost` 组件，行为不变。
- 自动压缩开始时上下文环显示进行中态，不再只在压缩完成后才有反馈；压缩无操作或失败也会给出终止信号，避免进行中态卡死。
- Grok ACP 官方账号路径连接前会先检查缓存 token 是否已过期，过期则直接提示重新登录，不再静默 spawn 后才发现认证失败。
- 项目 worktree 切换器新增新建/删除入口（此前只能列出并绑定已存在的 worktree）。
- Full Access（YOLO）权限策略新增危险命令拦截：`rm -rf`、`git reset --hard`、`git clean -f*`、非 `--force-with-lease` 的强制推送等仍会弹出确认，不再无条件自动放行；权限卡片对危险命令加醒目提示。
- 权限卡片里过长的命令预览可展开查看完整内容，不再只能在 3 行小窗口里滚动。
- `wait_commands` / `command_output` 的 `timeout_ms` 增加 1 小时上限，避免模型传入超大值导致单次工具调用长时间阻塞。

## [1.0.2] — 2026-09-08 00:30

- 输入器显示当前会话正在跑的后台命令数量；其他会话有待批准请求时顶部出现横幅，点击直接跳过去。
- 对话里可用 loop_start 开一个绑定当前会话的循环，每隔至少 60 秒无用户气泡地唤醒自己继续任务，7 天后自动过期，也可 loop_cancel 提前停止。
- 设置会标明开发模拟或环境变量盖过了已保存的内核偏好；Grok ACP 适配器仍留在显式开关后。
- 内建内核的长流式不再被 2 分钟总超时打断，命令默认可跑 15 分钟，工具轮次用尽后可以再发送而不是当成崩溃。
- 对话里可用后台 run_command 托管长命令，完成后会唤醒同一会话继续。
- 对话里可用 monitor 订阅一个仍在运行的后台命令，新行匹配（或任意新行）时唤醒同一会话，不必轮询。
- Windows AppContainer 命令沙箱能按锁定的 windows 0.61 绑定编译。
- 键盘焦点环画在控件内侧，空会话建议卡在矮窗口不再被输入器挡住。
- 键盘 Tab 和关闭侧栏/文件面板后会显示可见焦点环；顶栏按钮不再放在窗口拖动区域内。
- 侧栏和顶栏会话菜单可以把对话移动到已有项目，或改为不归属项目。
- 到期任务由 Host 在后台点火，不必等 WebView 空闲，也不会抢走当前对话。
- 设置可注册登录后的后台调度，退出后仍能按间隔唤醒跑已安排任务。
- 内建内核的命令可按设置使用 Linux bubblewrap、macOS sandbox-exec 或 Windows AppContainer 隔离。
- 对话里可用 skill_save 在授权后写入用户或项目 Skill，不能覆盖插件 Skill。
- 内核可按需列出并加载已启用 Skill，不必只靠输入器芯片。
- 内建内核可自动保存并注入有界笔记和用户画像；设置里可开关和清空，且与已审阅候选分开。
- 对话里可用 schedule_task 创建已安排任务，并在触发时加载附带 Skill。
- 市场可用内部集成令牌连接 Notion，并在对话中搜索或创建页面。
- 市场可用机器人令牌连接 Slack，并在对话中阅读频道或发帖。
- 默认内核支持 `/compact` 与自动压缩，可见对话不会被删改。
- 后台子代理结束后会唤醒父会话继续；Steer 只结束本轮子代理。
- 握手测试与当前 1.0.1 版本对齐；隔离 home 的测试不再互相抢环境变量。

## [1.0.1] — 2026-08-29 20:10

- 默认使用内建 Agent 内核，进入工作台不再需要 Grok CLI。
- 可信项目内可读写、搜索、替换和运行命令；写入和命令需授权。
- 对话中可启动子代理，并查看只读记录。
- 显式技能和已审阅记忆只会按选择注入当前轮。
- 计划可恢复，并与权限审阅分开。
- 提问会打开底部提问框；发送后立刻显示自己的气泡。
- GitHub、Gmail、Drive、Calendar 可在应用内连接。
- API Key、MCP 和连接器凭据只保存在当前用户本机。
- 设置新增「我的模型」；官方统一模型仍需自行配置 API。
- 生成时思考动效固定在本轮最底下。
- 侧栏、输入器、资源舱和空会话做了密度对齐。
- 首次启动改为登录或跳过。
- 已安排任务仅在进程确认退出后才会补跑。

## [1.0.0] — 2026-07-27 11:55

- 建立 Sunsetz 私有远程仓库并重写中英文项目说明。
- 完成工作台侧栏、顶部栏、响应式资源面板和焦点恢复基础重构。
- 引入 HostCapabilities v2，隐藏未实现或不可用的功能入口。
- 建立前端覆盖率、契约检查、视觉回归和三平台 Rust 持续集成。
- 清理与 Sunsetz 品牌无关的截图、二维码、链接和过期文档。
- 修复 Windows 上技能目录原子保存失败的问题。
